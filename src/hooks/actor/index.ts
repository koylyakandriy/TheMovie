// import { useQuery } from 'react-query';
// import { QUERY_KEYS } from '../../queryKeys';
// import { order } from '../Order';

// export const useOrderSummary = () =>
// 	useQuery(
// 		QUERY_KEYS.orderSummary,
// 		async () => (await order.getOrderSummary()).data,
// 	);
